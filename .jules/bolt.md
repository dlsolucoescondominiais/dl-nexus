## 2024-05-24 - [Optimize KPI Calculation in Dashboard]
**Learning:** Reducing multiple O(N) `.filter().length` array iterations to a single O(N) `.reduce()` pass is an effective and safe optimization for frontend data aggregation.
**Action:** Continuously look for opportunities to consolidate multiple array iterations into a single pass when aggregating metrics in React components.
