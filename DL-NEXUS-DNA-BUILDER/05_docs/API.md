# DNA-Builder - Documentação da API Interna

Endpoint Principal: `POST /api/v1/workflows/build` (via n8n Webhook no workflow DNA_Builder_Engine)

Payload:
```json
{
  "instruction": "create a workflow with an http request"
}
```
