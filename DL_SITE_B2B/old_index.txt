
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>DL Soluções Condominiais | Avaliação Técnica para Condomínios e Empresas</title>

  <meta
    name="description"
    content="DL Soluções Condominiais: CFTV, controle de acesso, automação, elétrica predial, energia solar, carregadores veiculares, prevenção de incêndio e suporte técnico para condomínios e empresas no Rio de Janeiro."
  />

  <meta name="robots" content="index, follow" />
  <meta name="theme-color" content="#0b1f33" />

  <style>
    :root {
      --dl-blue-dark: #0b1f33;
      --dl-blue: #123a5f;
      --dl-blue-light: #1d5d8f;
      --dl-gold: #d9a441;
      --dl-gray-50: #f7f9fb;
      --dl-gray-100: #eef2f6;
      --dl-gray-200: #d8e0e8;
      --dl-gray-600: #5b6b7c;
      --dl-gray-800: #253444;
      --dl-white: #ffffff;
      --dl-danger: #b42318;
      --dl-success: #087443;
      --shadow-soft: 0 16px 40px rgba(11, 31, 51, 0.12);
      --radius-lg: 22px;
      --radius-md: 14px;
      --max-width: 1180px;
    }

    * {
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
      color: var(--dl-gray-800);
      background: var(--dl-gray-50);
      line-height: 1.55;
    }

    img {
      max-width: 100%;
      height: auto;
      display: block;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    .container {
      width: min(100% - 32px, var(--max-width));
      margin-inline: auto;
    }

    .skip-link {
      position: absolute;
      top: -48px;
      left: 16px;
      padding: 10px 14px;
      background: var(--dl-gold);
      color: var(--dl-blue-dark);
      z-index: 999;
      border-radius: 8px;
      font-weight: 700;
    }

 
<truncated 45214 bytes>
          }

          const aceiteContato = document.getElementById("aceite_contato");
          const aceiteLgpd = document.getElementById("aceite_lgpd");

          if (aceiteContato) {
            aceiteContato.checked = true;
          }

          if (aceiteLgpd) {
            aceiteLgpd.checked = true;
          }
        } catch (error) {
          setFormMessage(
            "error",
            "Não foi possível enviar sua solicitação agora. Você também pode falar com o comercial pelo WhatsApp."
          );

          window.trackEvent("erro_formulario", {
            motivo: "erro_webhook",
            mensagem: sanitize(error.message),
            pagina_origem: window.location.href
          });
        } finally {
          setSubmitState(false);
        }
      }

      document.addEventListener("DOMContentLoaded", function () {
        saveCampaignData();
        attachClickTracking();

        const form = document.getElementById("leadForm");

        if (form) {
          form.addEventListener("submit", handleSubmit);
        }
      });
    })();
  </script>
</body>
</html>
